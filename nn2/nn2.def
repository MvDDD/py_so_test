struct __fsid_t {
    <class 'pycparser.c_ast.ArrayDecl'> __val;
}

struct NN {
    i32 depth;
    ptr<i32> layer_sizes;
    ptr<ptr<f32>> weights;
    ptr<ptr<f32>> biases;
    i32 _error;
}

func NN__error(nn:ptr<NN>) -> i32
func NN_create(nn:ptr<NN>, depth:i32, layer_sizes:ptr<i32>) -> void
func NN_destroy(nn:ptr<NN>) -> void
func NN_forward(nn:ptr<NN>, input:ptr<f32>, output:ptr<f32>) -> void
